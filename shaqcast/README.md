# shaqcast

Minimal GUI tool (wxPython) that listens to a selected Windows output device (loopback), recognizes the current track (Shazam), and updates Shoutcast "Now Playing" metadata for one or more SIDs.
